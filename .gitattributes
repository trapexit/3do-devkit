* text=auto
*.doc text
*.make text
*.c text
*.cp text
*.cpp text
*.h text
*.hpp text
# PIMAP assets are CR-delimited data consumed by music.lib ReadStreamLine
# (EOL is '\r'); never apply newline normalization.
*.pimap -text
